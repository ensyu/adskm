# ADSKM MVP Dependencies

# Core
Python>=3.12.0

# Data validation & serialization
pydantic>=2.0.0
pydantic-core>=2.0.0
pyyaml>=6.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Development tools
black>=23.0.0
flake8>=6.0.0
mypy>=1.4.0
isort>=5.12.0

# Logging & utilities
python-dotenv>=1.0.0

# Future dependencies (not in MVP)
# FastAPI for API server (future)
# SQLAlchemy for database (future)
# alembic for migrations (future)
# click for CLI (future)
# rich for terminal output (future)
